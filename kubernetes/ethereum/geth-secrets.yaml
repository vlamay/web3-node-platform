apiVersion: v1
kind: Secret
metadata:
  name: geth-secrets
  namespace: web3
  labels:
    app: geth
    component: ethereum-node
type: Opaque
stringData:
  # RPC Authentication (if needed)
  rpc-jwt-secret: "change-me-in-production"
  
  # Node Private Key (optional, auto-generated if not provided)
  # Uncomment and set for persistent node identity
  # node-key: ""
